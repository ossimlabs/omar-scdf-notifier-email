spring:
  application:
    name: omar-scdf-notifier-email
  cloud:
    config:
      enabled: true
      uri: ${SPRING_CONFIG_URI:http://localhost:8888/scdf-config-server}
    discovery:
      enabled: true
